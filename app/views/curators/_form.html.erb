<%= form_for(@curator) do |f| %>
  <% if @curator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curator.errors.count, "error") %> prohibited this curator from being saved:</h2>

      <ul>
      <% @curator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shortname %><br>
    <%= f.text_field :shortname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
