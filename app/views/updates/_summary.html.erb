<dl class="update">
  <dt>Time</dt>
  <dd><time datetime="<%= update.time.to_s %>"><%= update.time.to_s %></time></dd>
  <% update.elements.select{ |k, v| !v.nil? && !v.to_s.empty? }.each do |k, v| %>
    <dt><%= k.name %></dt>
    <dd><%= v %></dd>
  <% end %>
</dl>

